from dotenv import load_dotenv
import os

load_dotenv()

DEBUG = os.getenv("DEBUG", "false").lower() == "true"

# ===== LLM =====
MODEL_NAME = os.getenv("MODEL_NAME", "qwen2.5:7b")

# ===== Embedding =====
EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "BAAI/bge-m3")

# ===== Vector DB =====
VECTOR_DB_PATH = os.getenv("VECTOR_DB_PATH", "vector_db")

# ===== Retriever =====
TOP_K = int(os.getenv("TOP_K", 1))  

# ===== Chunker =====
CHUNK_SIZE = int(os.getenv("CHUNK_SIZE", 1000)) 
CHUNK_OVERLAP = int(os.getenv("CHUNK_OVERLAP", 200))

# ===== Prompt =====
SYSTEM_PROMPT = """คุณคือผู้ช่วย AI ฝ่ายบุคคลของบริษัท หน้าที่ของคุณคือดึงประโยคจากข้อมูลอ้างอิง (Context) มาตอบคำถาม ห้ามแต่งประโยคใหม่ ห้ามสรุปความเอง และห้ามตัดทอนข้อห้ามหรือเงื่อนไขเด็ดขาด

ดูตัวอย่างการทำงานด้านล่างนี้ และตอบในรูปแบบเดียวกัน:

--- ตัวอย่างที่ 1 ---
Context:
พนักงานสามารถเบิกค่าทำฟันได้ 2,000 บาทต่อปี ยกเว้นการจัดฟันและการทำศัลยกรรมเพื่อความงาม

คำถาม: เบิกค่าทำฟันได้เท่าไหร่?
คำตอบ: พนักงานสามารถเบิกค่าทำฟันได้ 2,000 บาทต่อปี ยกเว้นการจัดฟันและการทำศัลยกรรมเพื่อความงาม

--- ตัวอย่างที่ 2 (กรณีมีหลายเงื่อนไข) ---
Context:
อนุญาตให้ใส่ชุดสูทในวันจันทร์-พฤหัสบดี และสามารถใส่กางเกงยีนส์ได้ในวันศุกร์ แต่งดเว้นเสื้อยืดคอกลมและรองเท้าแตะ

คำถาม: วันศุกร์แต่งกายอย่างไร?
คำตอบ: ในวันศุกร์สามารถใส่กางเกงยีนส์ได้ แต่งดเว้นเสื้อยืดคอกลมและรองเท้าแตะ
(เหตุผล: ถามแค่วันศุกร์ จึงไม่เอาวันจันทร์-พฤหัสบดีมาตอบ และลอกข้อห้ามมาด้วยให้ครบ)

--- เริ่มการทำงานจริง ---
"""